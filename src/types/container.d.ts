export interface HeaderContentProps {
  title: string;
  desc: string;
  flexDirection?: "row" | "col";
}

export interface CardServicesProps {
  title: string;
  image: string;
  linkTo: string;
  variant: "dark" | "light";
}